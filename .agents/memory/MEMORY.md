- [Desktop-web indexed-property crash](desktop-web-hydration-crash.md) — CONFIRMED: expo-router `<Link asChild>` leaks an RN style array onto the web `<a>` host → React DOM crash; fix = router.push + plain Pressable.
- [Vercel deploy stale build](vercel-deploy-stale-build.md) — prod "still broken" but code is fixed → compare sw.js VERSION across local/GitHub-main/live; if GitHub>live it's a Vercel deploy failure, not a code bug.
- [Budget is a hard cap on the look TOTAL](budget-hard-cap.md) — every displayed look's SUM of pieces must be ≤ selected budget, not just per-item; both generateLooks + generateLookFromAIPlan enforce it; never add a useBudget:false fill pass.
- [Promo code grant-tier UX trap](promo-codes.md) — grant_tier promos flip membership.tsx to the member-splash early-return; promo controls must live in BOTH branches.
- [Promo codes — two sources](promo-codes-two-sources.md) — built-in offline codes vs admin-created DB codes; redeem checks built-ins first; server codes snapshot effect at redeem (never re-derive) so owner edits don't revoke existing grants.
- [TV-inspiration look constraints](tv-inspiration-look-constraints.md) — TV looks: one item/category, season-coherent, article-dedup by productKey (not id); cross-look reservations commit only AFTER all gates or a dropped look poisons the batch.
- [api-server externalizes @google/*](api-server-external-google-deps.md) — esbuild leaves @google/* unbundled; SDKs used only via a workspace lib still crash at runtime (ERR_MODULE_NOT_FOUND) unless added as a DIRECT api-server dep. Typecheck won't catch it.
- [Catalog dedup + feed categories](catalog-dedup-and-categories.md) — outfit dedup must fingerprint on productKey(brand|name, colour stripped) NOT variant id (feed has per-colour rows); generated feed mislabels slots → re-infer category at load for sf_ items, never edit rows.
- [Look hero images](look-hero-images.md) — duplicate IMAGES ≠ duplicate combos; assignUniqueLookImages falls back to the look's OWN piece images (gender-safe, unique) once finite editorial pools exhaust.
- [Gender surfaces](gender-surfaces.md) — generated flow is gender-safe via itemMatchesGender; "still leaking" is usually a stale bundle; Shop's Product type has NO gender field so Shop can't be filtered without a schema change.
- [Outfit engine hard rules](outfit-engine-hard-rules.md) — (1) zero gender leakage: `["men","unisex"]` is a MEN item, never wildcard on unisex; one helper gates all pools. (2) no duplicate looks ever: all 4 gen paths dedup + persist fingerprints; await hydration before generating; never clear the set.
- [TV Inspiration completeness + dedup](tv-inspiration-completeness-dedup.md) — TV-muse looks: always top+bottom+shoes (no dress), exactly one item per category, no bag required, zero-duplicate items grid-wide; thread `tvInspiration` through every isCompleteOutfit gate (requireBag/requireSeparates) + both fallbacks + AI usedAcross/usedCategories.
- [TV Shows weekly-quota invariant](tv-shows-weekly-quota.md) — each tvShows.ts category pool must be > its WEEKLY_QUOTA or rotatePick silently degrades the top-10 (fewer items + no rotation); typecheck won't catch it.
- [parseBudget silent default](parsebudget-silent-default.md) — unrecognized budget tokens (e.g. "$$$") fall back to $500–$1500, starving the resolver → 0 looks → red error; luxury surfaces must pass "$6000+". Runway heroes use real piece.imageUrl, not editorial/localImage.
- [Outfit completeness contract](outfit-completeness.md) — no partial outfits; `isCompleteOutfit` gates every look-builder; women REQUIRE a bag; men's bags whitelisted (no totes — `isBagAppropriateForGender`); AI resolver backfills; composes with budget hard-cap; bagless brand-lock → honest empty.
- [Formal Remix sneaker alternative](formal-remix-sneaker-alt.md) — additive `sneakerAlt` (not in pieces/price), rule-based Formal Remix only; budget = SWAP headroom (budget − non-shoe total); ranked by price+prestige so pricier/limited-edition surface as budget grows.
